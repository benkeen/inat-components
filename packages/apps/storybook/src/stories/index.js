import React from 'react';
import { storiesOf } from '@storybook/react';
import Selector from '@benkeen/species-selector';

storiesOf('Species Selector', module).add('Example', () => (
	<div>
		Test.
		<Selector />
	</div>
));
